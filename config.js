// Optional runtime config for static hosting (Netlify, etc.).
// Copy this file and set values as needed.
window.LOCALSHARE_CONFIG = window.LOCALSHARE_CONFIG || {
  // PeerJS signaling override (optional):
  // peer: { host: "your-peer-host", port: 443, path: "/peerjs", secure: true },
};
